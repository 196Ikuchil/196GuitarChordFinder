import { Box } from '@material-ui/core';
import { PianoKeyboard } from '../../pianokeyboard';

export default function PianoKeyboardBox() {
  return (
    <Box>
      <PianoKeyboard />
    </Box>
  );
}
